package pl.piszkod.cinema.service

import munit.CatsEffectSuite

class ScheduleRepositorySpec extends CatsEffectSuite {

  test("should get room schedule") {}
  test(
    "should fail if attempts to get schedule for non-existent room"
  ) {}
  test("should get full schedule") {}
  test("should add new schedule slot to room") {}
  test("should fail if attempts to add slot for non-existent room") {}

}
